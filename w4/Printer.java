public interface Printer {
    // print is an abstract method
    // it has no implementation
    // any class that implements this interface must provide its own implementation (override)
    // for the interface's abstract methods
    void print(String s);
}
